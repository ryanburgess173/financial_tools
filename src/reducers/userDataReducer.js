const userDataReducer = (state=0, action) =>{
    switch(action.type){
        case 'ACTION1':
            return state;
        default:
            return state;
    };
};
export default userDataReducer;